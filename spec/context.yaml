name: "Templater CLI"
description: "Acceptance tests for the templater patch-based feature system"

env:
  TEMPLATER: "${SPEC_ROOT}/../templater"

before:
  run: |
    cd ${SPEC_ROOT}/..
    go build -o templater .
  timeout: 30s
